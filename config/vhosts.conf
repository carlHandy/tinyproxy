vhosts {
    # example.com {
    #     port 80
    #     root /var/www/html
    #     proxy_pass http://backend-server:8080
    #     compression on

    #     ssl {
    #         cert /etc/certs/example.com.crt
    #         key /etc/certs/example.com.key
    #     }

    #     security {
    #         frame_options SAMEORIGIN
    #         content_type nosniff
    #         xss_protection "1; mode=block"
    #         csp "default-src 'self'"
    #         hsts "max-age=31536000; includeSubDomains"
            
    #         rate_limit {
    #             requests 100
    #             window 1m
    #         }
    #     }

    #     socks5 {
    #         address 127.0.0.1:1080
    #         username "proxy_user"
    #         password "proxy_pass"
    #     }
    # }

    # api.example.com {
    #     port 443
    #     proxy_pass http://api-backend:3000
    #     compression on

    #     ssl {
    #         cert /etc/certs/api.example.com.crt
    #         key /etc/certs/api.example.com.key
    #     }

    #     security {
    #         frame_options DENY
    #         content_type nosniff
    #         xss_protection "1; mode=block"
    #         csp "default-src 'self' api.example.com"
            
    #         rate_limit {
    #             requests 50
    #             window 30s
    #         }
    #     }
    # }

    # php.example.com {
    #     port 80
    #     root /var/www/html

    #     fastcgi {
    #         pass 127.0.0.1:9000
    #         index index.php
    #         param SCRIPT_FILENAME /var/www/html/$fastcgi_script_name
    #         param PATH_INFO $fastcgi_path_info
    #     }
    # }

    # To enable bot and AI crawler protection, add a bot_protection block:
    # example.com {
    #     bot_protection {
    #         enabled true
    #         block_scanners true   # block .env, .git, wp-admin probes
    #         block PerplexityBot   # additional UA substrings to block
    #         allow FriendlyBot     # override: always allow this UA
    #     }
    # }

    # Load-balanced upstream with caching and cookie-based session affinity:
    # app.example.com {
    #     compression on
    #
    #     upstream {
    #         strategy cookie           # round_robin | least_conn | ip_hash | weighted | cookie
    #         cookie_name _tp_backend   # sticky-session cookie name (default: _tp_backend)
    #
    #         backend http://10.0.0.1:8080 weight 3
    #         backend http://10.0.0.2:8080 weight 2
    #         backend http://10.0.0.3:8080
    #
    #         health_check {
    #             path /healthz
    #             interval 10s
    #             timeout 5s
    #             fail_threshold 3    # consecutive failures to mark backend down
    #             pass_threshold 2    # consecutive passes to mark backend up
    #         }
    #     }
    #
    #     cache {
    #         enabled true
    #         max_size 256MB            # max in-memory cache size
    #         default_ttl 5m            # fallback TTL when no Cache-Control header
    #         stale_while_revalidate 30s
    #         bypass_header X-Cache-Bypass
    #     }
    # }

    localhost:8080 {
        root static
        bot_protection {
            enabled true
            block_scanners true
            honeypot true
        }
    }
}